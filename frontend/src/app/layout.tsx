import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Inbox Reaper - Clean Your Gmail Subscriptions Automatically",
  description: "Automatically detect, unsubscribe from, and bulk delete unwanted email subscriptions from your Gmail inbox. Secure OAuth2 integration.",
  keywords: "gmail, email, unsubscribe, subscriptions, email management, inbox cleaner",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
